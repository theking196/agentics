import { PlatformTokens, PlatformUser } from '../types';
import { OAuthPlatform, PlatformConfig } from '../types/platform';

export { OAuthPlatform, PlatformConfig };
export type { PlatformTokens, PlatformUser };
